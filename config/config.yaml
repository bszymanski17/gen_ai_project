llm:
  model: 'gemini-3.1-pro-preview'
  jailbreak_model: 'gemini-2.0-flash'

validation_approaches:
  generating_approach: function_calling   # direct / function_calling
  editing_approach: query    # direct / function_calling / query